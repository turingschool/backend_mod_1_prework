# Prints this declaration to the terminal
puts "I will now count my chickens:"

# Prints "Hens 30". 30 is some interpolated math.
puts "Hens #{25.0 + 30.0 / 6.0}"
# Prints "Roosters 97". 30 is some interpolated math.
puts "Roosters #{100.0 - 25.0 * 3.0 % 4.0}"

# Prints this declaration to the terminal
puts "Now I will count the eggs:"

# Prints the answer to all that math.
puts 3.0 + 2.0 + 1.0 - 5.0 + 4.0 % 2.0 - 1.0 / 4.0 + 6.0

# Prints this statement, but doesn't do the math because it's a string.
puts "Is it true that 3.0 + 2.0 < 5.0 - 7.0?"

# Prints the answer to all that math.
puts 3.0 + 2.0 < 5.0 - 7.0

# Prints the question, and the answer.
# The answer is caluated because of string interpolation.
puts "What is 3.0 + 2.0? #{3.0 + 2.0}"
# Prints the question, and the answer.
# The answer is caluated because of string interpolation.
puts "What is 5.0 - 7.0? #{5.0 - 7.0}"

# Prints a sentence.
puts "Oh, that's why it's false."

# Prints another sentence.
puts "How about some more."

# Prints the question, and the answer.
# This time the interpolated answer is boolean algebra, so it's true.
puts "Is it greater? #{5.0 > -2.0}"
# Prints the question, and the answer.
# This time the interpolated answer is boolean algebra, so it's true.
puts "Is it greater or equal? #{5.0 >= -2.0}"
# Prints the question, and the answer.
# This time the interpolated answer is boolean algebra, so it's false.
puts "Is it less or equal? #{5.0 <= -2.0}"

# Study Drills:
# 1. Done
# 2. Done
# 3. see something_i_need_to_calculate.rb
# 4. Done
