# outputs this string
puts "I will now count my chickens:"
# outputs this string with this arithmetic interpolated into it
puts "Hens #{25.0 + 30 / 6}"
# outputs this string with this arithmetic interpolated into it
puts "Roosters #{100.0 - 25 * 3 % 4}"
# outputs this string
puts "Now I will count the eggs:"
# outputs the answer to this operation which is 7
puts 3.0 + 2 + 1 - 5 + 4 % 2 - 1 / 4 + 6
# outputs this string
puts "Is it true that 3.0 + 2 < 5.0 - 7?"
# outputs the answer to this comparison which is false
puts 3.0 + 2 < 5.0 - 7
# outputs this string with this arithmetic interpolated into it
puts "What is 3.0 + 2? #{3.0 + 2}"
# outputs this string with this arithmetic interpolated into it
puts "What is 5.0 - 7? #{5.0 - 7}"
# outputs this string
puts "Oh, that's why it's false"
#outputs this string
puts "How about some more"
# outputs this string with this comparison interpolated into it
puts "Is it greater? #{5.0 > -2}"
# outputs this string with this comparison interpolated into it
puts "Is it greater or equal? #{5.0 >= -2}"
# outputs this string with this comparison interpolated into it
puts "Is it less or equal? #{5.0 <= -2}"


# new calculation which equals 2
puts 17 % 15
# new calculation which equals 0.75
puts 3.0 / 4