
# this variable is equal to 10
types_of_people = 10
# this x variable is a string with the previous variable inside
x = 'There are #{types_of_people} types of people.'
# this variable is equal to a string
binary = 'binary'
# this variable is equal to a string
do_not = "don't"
# this variable is a string with the previous 2 variables inside
y = 'Those who know #{binary} and those who #{do_not}.'

# These lines pring the stated variables
puts x
puts y
# These lines print the variables x and y (which are also strings) inside of the strings
puts 'I said: #{x}.'
puts "I also said: '#{y}'."
# this variable is a boolean but also acts as a string
hilarious = false
# this variable is a string with the previous variable inside of the string
joke_evaluation = "Isn't that joke so funny?! #{hilarious}"
# this line prints the previous variable string statement
puts joke_evaluation
# these are two more variables that are strings
w = "This is the left side of..."
e = "a string with a right side."
# this statement prints the previous variables (which are equal to strings) together as one string
puts w + e

# Study Drills
# 4: the line is saying to put the w statement before the e statement as one sentence (or string)
# 5: The work for the for the variables or puts statements that don't have a ' already in it. Such as the do_not variable doesn't work becsuse don't has a ' in it. But the x variable is just fine with single quotes and not double quotes.
